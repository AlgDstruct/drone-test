name: C

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: make
      run: make
  
  memtest:
    runs-on: ubuntu-latest
    steps:
    - name: memcheck
      run: valgrind ./a.out
